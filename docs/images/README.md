# Screenshots

Drop the two captures here with these exact names so the README picks them up:

- `chat-ui.png`      — the Harbor chat view with cache-hit / route badges
- `grafana.png`      — the Grafana "Harbor — Gateway" dashboard (capture within
                        ~30s of `make bench` so the hit-ratio gauge shows the
                        live rate, not a rolled-off 0%)

Optional:
- `quality-dashboard.png` — the React "Quality" tab (eval trends + run log)
